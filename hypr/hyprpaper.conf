# hyprpaper draws Hyprland's splash text ("Thanks raf!" etc.) over the wallpaper by
# default — turn it off. No preload/wallpaper lines here: 0.8.4's preload is broken, so
# the wallpaper itself is set over IPC by scripts/wallpaper.sh.
splash = false
ipc = on
